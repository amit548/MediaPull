{
  "name": "youtube_downloader",
  "version": "0.0.0",
  "private": true,
  "packageManager": "npm@10.9.2",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix apps/server\" \"npm run dev --prefix apps/client\"",
    "start": "concurrently \"npm run start --prefix apps/server\" \"npm run start --prefix apps/client\"",
    "build": "npm run build --prefix apps/client && npm run build --prefix apps/server"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
