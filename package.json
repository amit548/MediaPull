{
  "name": "youtube_downloader",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "packageManager": "npm@10.9.2",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix apps/client\" \"npm start --prefix apps/electron\"",
    "build": "npm run build --prefix apps/client && npm run make --prefix apps/electron",
    "lint": "npm run lint --prefix apps/client"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
